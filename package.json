{
  "name": "upi-payment-experiment-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test:frontend": "npm run build && node --test frontend/tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
